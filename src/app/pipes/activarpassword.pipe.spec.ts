import { ActivarpasswordPipe } from './activarpassword.pipe';

describe('ActivarpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivarpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
